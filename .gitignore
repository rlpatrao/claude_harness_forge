test-projects/
.DS_Store
.vscode/
.playwright-mcp/

# BRD v3.1 §4 runtime artifacts (comments on separate lines — .gitignore
# does NOT support inline comments; a trailing "# foo" becomes part of
# the pattern and matches nothing).

# v3.1.6 CCR per-hash caches
state/context-cache/*/
!state/context-cache/README.md

# v3.1.9 in-flight ledger (drained on Stop)
state/dirty-files.jsonl

# v3.1.5 concurrency ledger
state/concurrency-ledger.jsonl

# v3.1.6 per-session state
state/token-advisor-session.json

# v3.3 correction stream (mined into rule-candidates)
state/rejections.jsonl

# v3.3 correction-detector outputs (curated via /rules)
state/rule-candidates/*.json
!state/rule-candidates/.gitkeep

# v3.3 dedup ledger
state/.rule-candidate-hashes.txt

# cleanup-phase-2 (2026-07-21) hook fire log
state/fire-log.jsonl

# Generated by scripts/jscpd-gate.js self-scan (do not commit)
specs/reviews/jscpd-report.json
