================================================================
claude_harness_forge — harness progress log
================================================================

Project: claude_harness_forge
Repo: rlpatrao/claude_harness_forge
Spec: brd/v3.0.md (supersedes architecture.md, forge-reference.md, program.md)
Plan: brd/v3.0-implementation-plan.md
Contract: feature_list.json (BRD §3.2)

================================================================
ARCHITECTURE DECISIONS (frozen at v3.0 BRD persist)
================================================================

D1. Retrofit, not rewrite. v2.0 multi-cloud adapter, Durable Functions
    topology, permission classifier, and existing 41 skills stay.

D2. The five v3.0 design principles (BRD §1) override prior conventions
    where they conflict:
      - Separation of concerns
      - Progressive degradation
      - Transparency over magic
      - Goal-driven, not instruction-driven
      - Compound system over monolithic agent

D3. Initializer agent runs once at project genesis (BRD §3.1).
    Coding-agent runs every subsequent session; obeys the 8-step
    SessionStart sequence enforced by hooks/session-start.js.

D4. feature_list.json is the source of truth for completion (BRD §3.2).
    Schema: { id, category, description, steps[], passes: false,
              source_section, depends_on[], verification_artifact_path }
    Entries are append-only (no delete); only the `passes` field flips,
    and only after a verification artifact lands under verification/<id>.

D5. Source attribution lives in BRD §10 + Appendix A (canonical) plus
    inline `source:` frontmatter on each adapted file under agents/,
    skills/, hooks/. No separate vendor/ directory or NOTICE.md is
    maintained — features are assimilated into the codebase. The BRD's
    §10.6 vendoring-discipline requirements are deferred; re-engage
    only if we ever redistribute upstream code verbatim and the
    license obligates it. (Reversal noted 2026-05-19 by @rlpatrao.)

D6. Per-workflow LLM routing (BRD §3.4) replaces all-agents-on-Opus.
    Source of truth lives in config/workflows.yaml (created in increment 2a).

D7. Plan Mode is a subagent with a read-only tool schema (BRD §3.5),
    not a state machine. The schema restriction makes "stuck in plan
    mode" structurally impossible.

D8. Five-layer defense-in-depth safety (BRD §4.1). The existing 19
    hooks slot in as Layer 5. Layer 3 reuses the existing permission
    classifier.

D9. State files already exist for v3.0 components:
      state/eval-scores.json    → §4.8 snapshot store
      state/cost-log.json       → §6.1 cost ledger
      state/iteration-log.md    → §4.8 experiment log
      templates/features-template.json → §3.2 template seed
      templates/init-sh.template       → §3.1 init.sh template
      hooks/cost-tracker.js     → §3.7 budget footer foundation
      hooks/token-budget.js     → §3.7 budget footer foundation

D10. Branch policy. All v3.0 retrofit work happens on
     claude/implement-claude-agents-aSXLm (this branch) until merged to
     main.

================================================================
PROGRESS LOG (append-only)
================================================================

[2026-05-19 | session: initial-persistence | branch: claude/implement-claude-agents-aSXLm]
- Persisted BRD v3.0 to brd/v3.0.md.
- Created brd/README.md (index) and brd/v3.0-implementation-plan.md (operational plan).
- Seeded feature_list.json with 47 entries covering every BRD §3 / §4 / §5 retrofit item.
- Seeded harness-progress.txt (this file) with architecture decisions D1–D10.
- Created vendor/README.md (BRD §10 sync ledger) and NOTICE.md (attribution stub).
- Increment 1a delivered:
    * agents/initializer.md, agents/coding-agent.md
    * hooks/session-start.js, hooks/feature-edit-guard.js
    * settings.json updated to register SessionStart and feature-edit-guard
- Verification: jq on feature_list.json passes; both new hooks dry-run exit 0;
  no existing tests broken (no existing tests modified).
- Commit: 457ba1b

[2026-05-19 | session: increment-1-complete | branch: claude/implement-claude-agents-aSXLm]
- Increment 1b (BRD §3.8) E2E gate:
    * hooks/e2e-gate.js — PreToolUse Edit|Write; rejects passes-flip without
      a non-empty, git-tracked verification artifact
    * verification/.gitkeep
    * settings.json: e2e-gate.js BEFORE feature-edit-guard.js in PreToolUse
- Increment 1c (BRD §3.3) Ralph Loop:
    * hooks/ralph-loop.js — Stop hook; returns decision:block when failing
      features remain; per-session intercept counter yields after 3
- Verification: both hooks node -c clean; e2e-gate.js correctly blocks
  artifact-less flip with exit 2 and BRD §3.8 message
- Commit: 1c5bf25

[2026-05-19 | session: increment-2 | branch: claude/implement-claude-agents-aSXLm]
- Increment 2a (BRD §3.4) per-workflow LLM routing:
    * config/workflows.yaml — 13 workflows with primary, failover[],
      thinking_level, max_iterations, tools_filter
- Increment 2b (BRD §3.5) Plan Mode as subagent:
    * agents/planner.md (read-only schema)
    * commands/plan.md
    * scratch/plans/.gitkeep
- Increment 2c (BRD §3.6) Extended ReAct + Critic + Compactor:
    * agents/critic.md (GAN-pair critic, Opus, read-only)
    * agents/compactor.md (Haiku, summarize-only)
    * skills/extended-react/SKILL.md
    * hooks/compaction-stage.js — PreCompact hook; stages 1-5 with
      spawn_subagent: compactor directive at stages 3-5
- Increment 2d (BRD §3.7) tool-result budget footer:
    * hooks/budget-footer.js — PostToolUse all matchers; reads
      state/cost-log.json; computes regime (NORMAL/CONSERVE/HIGH/CRITICAL);
      appends footer via hookSpecificOutput.appendToToolResult
    * settings.json: PreCompact block; budget-footer registered LAST in
      PostToolUse so it sees fresh tokens
- Verification: workflows.yaml parses; budget-footer at 75% returns
  CONSERVE regime; compaction-stage at 80% returns stage 3 with
  preserve_last_turns:10
- Commit: a933388

[2026-05-19 | session: increment-3 + topology | branch: claude/implement-claude-agents-aSXLm]
- Increment 3a (BRD §4.7) spec backprop:
    * agents/spec-auditor.md (Opus, read-only)
    * commands/spec-audit.md
    * skills/spec-backprop/SKILL.md
- Increment 3b (BRD §4.8) monotonic-improvement guards:
    * hooks/experiment-logger.js — PostToolUse Bash matcher; fires after
      validate-evals.sh; compares latest snapshot to previous; applies
      keep-on-any-improvement | revert-on-any-regression | keep-always
      policy from state/monotonic-policy.json; appends to experiments/log.jsonl
    * experiments/log.jsonl (empty, initialized)
- Increment 3c (BRD §4.4) instinct extraction:
    * hooks/instinct-extractor.js — Stop hook (after ralph-loop); mines
      tool-sequence patterns of length 2-4; scores via 0.4 frequency +
      0.4 success_rate + 0.2 novelty; promotes >=0.5 score to
      instincts/pending/
    * instincts/{pending,tentative,confirmed}/.gitkeep
    * skills/instinct-extraction/SKILL.md
    * commands/{instinct-status,evolve,instinct-export,instinct-import}.md
- Increment 3d (BRD §4.5) tree-structured sessions:
    * skills/tree-sessions/SKILL.md
    * commands/{tree,fork,branch,export}.md
    * sessions/.gitkeep
- Increment 3e (BRD §6.5) recipe runner:
    * recipes/README.md (schema doc)
    * recipes/example-weekly-consulting.yaml (BRD §6.5 verbatim)
    * commands/recipe-run.md
- Remaining §5 topology:
    * agents/{e2e-runner,doc-updater}.md
    * skills/{iterative-retrieval,cross-provider-handoff}/SKILL.md
    * commands/{feature-add,feature-status,model,cost}.md
- settings.json: instinct-extractor registered in Stop after ralph-loop;
  experiment-logger registered in PostToolUse Bash after network-egress.

STATUS as of this commit:
- All 3 BRD §8 increments delivered.
- 47 feature_list.json entries remain at passes:false (the E2E gate
  blocks flips until verification artifacts are committed; this is by
  design — the retrofit code lands first, the entry flips when the
  corresponding component is exercised end-to-end in a later session).
- 6 new agents in agents/: initializer, coding-agent, planner, critic,
  compactor, spec-auditor, e2e-runner, doc-updater (=8 new total).
- 8 new hooks in hooks/: session-start, feature-edit-guard, e2e-gate,
  ralph-loop, compaction-stage, budget-footer, instinct-extractor,
  experiment-logger.
- 5 new skills in skills/: extended-react, spec-backprop, instinct-extraction,
  tree-sessions, iterative-retrieval, cross-provider-handoff (=6 net new
  given the planning).
- 14 new commands in commands/: plan, feature-add, feature-status,
  tree, fork, branch, export, instinct-status, evolve, instinct-export,
  instinct-import, spec-audit, model, cost, recipe-run (=15).
- 1 new config: config/workflows.yaml.
- Infrastructure dirs: verification/, scratch/plans/, instincts/{pending,
  tentative,confirmed}/, experiments/, sessions/, recipes/.

[2026-05-19 | session: gap-closure | branch: claude/implement-claude-agents-aSXLm]
- BRD §4.1 Layer-4 dangerous-patterns hook (rm -rf /, mkfs, dd>/dev/,
  curl|sh, chmod 777 /, git --force on protected branches, --no-verify,
  --no-gpg-sign, writes to .env/*.pem/*.key). Registered in PreToolUse
  for Edit|Write and Bash. Functional test: blocks rm -rf / with exit 2;
  allows rm /tmp/foo.txt.
- BRD §4.2 reminder-injector hook + prompts/reminders/ content
  (edit-production-code, destructive-bash, feature-passes-flip,
  subagent-spawn, checkpoint-due). Functional test: flipping
  passes:false→true on feature_list.json triggers the
  feature-passes-flip reminder.
- BRD §4.6 scripts/skills-loader.js (three-tier resolver — project,
  user, built-in priority). Functional: lists all skills correctly.
- BRD §3.4 scripts/workflows-resolver.js (reads config/workflows.yaml,
  resolves {{model:<workflow>}} placeholders). Functional: 13 workflows
  list correctly; critic → claude-opus-4-7; failover array resolves.
- BRD §4.5 scripts/tree-sessions.js — init, append, fork, branch,
  switch, tree, export commands. Functional smoke: built a 3-turn
  session with two labeled branches, rendered correctly.
- BRD §4.7 scripts/spec-backprop.js — emits packet for the Spec-Auditor
  subagent containing failing phase, feature entry, walked phase
  artifacts.
- BRD §6.5 scripts/recipe-runner.js — parses YAML recipe, interpolates
  inputs, emits execution plan, flags missing skills with exit 2.
  Functional: example recipe parses with all 4 steps + interpolated
  inputs; flagged 4 missing example skills correctly.
- BRD §4.4 scripts/instinct-evolve.js — lifecycle management
  (status, promote-pending, promote-tentative, prune-pending, cluster).
- /feature-status runtime: scripts/feature-status.js.
- /cost runtime: scripts/cost-render.js with fallback pricing table.
- Shared YAML mini-parser: scripts/yaml-mini.js (replaces two broken
  inline parsers).
- BRD §4.8 schema extension on state/eval-scores.json (snapshot store
  with checkpoint_id, git_sha, feature_list_hash, scores{},
  decision); state/monotonic-policy.json with default
  revert-on-any-regression policy.
- templates/features-template.json — 5-entry schema skeleton (was []).
- Supersession notices on agents/generator.md and agents/evaluator.md.
- Vendor UPSTREAM.md stubs for the 5 most-relied-on rows:
  claude-quickstarts, opendev-py, pi-ai, everything-claude-code, forge.
- NOTICE.md "upstream-stub" section listing adaptation locations.
- vendor/README.md ledger: 5 rows flipped to upstream-stub; 10 remain
  not-yet-synced.
- package.json declared (private; deps for pi-ai + claude-agent-sdk +
  Playwright/Puppeteer MCP servers + agentshield dev). Install
  deferred per vendor-first mandate; hooks/scripts are stdlib-only.
- settings.json: dangerous-patterns + reminder-injector registered in
  PreToolUse Edit|Write, Bash, Agent|Task matchers.

ALL BRD §3, §4 gap-closure scaffolding now has a working code path.
47 feature_list.json entries still passes:false (by design — e2e-gate
requires verification artifacts; the first dogfood run will flip
v3-3.1-initializer-split first).

[2026-05-19 | session: vendor-cleanup | branch: claude/implement-claude-agents-aSXLm]
- Per @rlpatrao: vendor/ directory and NOTICE.md removed. Features are
  assimilated into the codebase; source attribution lives in BRD §10 +
  Appendix A plus inline `source:` frontmatter on adapted files.
- Removed: vendor/ (5 stubbed UPSTREAM.md + 5 fetched-upstream copies),
  NOTICE.md.
- Updated D5 architecture decision to reflect the reversal.
- Stripped "officially vendored under vendor/" prose from agents/
  initializer.md, coding-agent.md, planner.md, compactor.md,
  spec-auditor.md, and from skills/spec-backprop, extended-react,
  instinct-extraction, tree-sessions, cross-provider-handoff, and
  from recipes/README.md and commands/cost.md.
- Removed vendor/pi-ai/pricing.json path check from scripts/cost-render.js
  (pricing table is now solely in-script, sourced from Pi-AI per BRD §6.1).
- brd/README.md updated: "Provenance" section replaces vendor-tracking.
- brd/v3.0-implementation-plan.md updated: "Provenance" section replaces
  "Vendor map (cross-cutting)".
- CLAUDE.md "Repo Structure" entry for vendor/ removed; added a
  "Provenance" section.
- brd/v3.0.md left untouched — it is the canonical historical spec.

NEXT (separate work, not blocking on this branch):
- Actual vendoring (UPSTREAM.md stubs → real file copies + LICENSE +
  patches/<repo>.patch). Start with claude-quickstarts.
- First end-to-end dogfood: pick v3-3.1-initializer-split, run the
  Initializer agent against a throwaway project, capture a verification
  artifact under verification/, exercise the full retrofit loop, flip
  the passes field. That validates the entire chain end-to-end.
- Wire the orchestrator-side translation of the new hook directives
  (hookSpecificOutput.spawn_subagent for compaction, decision:block for
  Ralph Loop, additionalContext for reminders). The hooks emit the
  contract; the runtime needs to honor it.
- npm install pi-ai once a Node entrypoint actually consumes it (today
  config/workflows.yaml is declarative-only).

================================================================
[2026-06-11 | session: v3.1-scaffold-import-and-review-loop | branch: main]
================================================================

BRD v3.1 milestone — first two increments landed.

Delivered v3.1.1 (Scaffold Q0: Q&A vs artifacts vs hybrid)
- New skill: skills/scaffold-import/SKILL.md + templates/import-checklist.md
- commands/scaffold.md: Step 0 inserted with Branches A / B / C
- skills/brd/SKILL.md: Step 0 sentinel check on specs/brd/.imported
- agents/brd-creator.md: Phase 0 pre-flight sentinel check
- skills/architect/SKILL.md: Step 0 mode detection reads specs/design/.imported

Delivered v3.1.2 (Architect synthesis mode + review loop + auto-handoff)
- New skill file: skills/architect/synthesis-mode.md
- New template: skills/architect/templates/architecture-review.md
- skills/architect/SKILL.md: Step 6 review loop (approve/amend/restart,
  3-cycle amend budget); Step 5 learnings deferred until Step 6 approval
- agents/architect.md: Phase 0 mode detection + Phase 6 review loop
- commands/architect.md: accepts --from-import and --restart flags
- hooks/session-start.js: reads state/architecture-approved.flag,
  surfaces "Suggested next command: /auto" in SessionStart reminder

feature_list.json: 47 → 50 entries (append-only, all new passes:false).
Added ids:
  v3.1.1-scaffold-import-mode      (source: BRD v3.1 §2)
  v3.1.2-architect-synthesis-mode  (source: BRD v3.1 §3, deps: 3.1.1-scaffold)
  v3.1.2-architect-review-loop     (source: BRD v3.1 §3)

Plan doc: brd/v3.1-implementation-plan.md refreshed with 4 new proposed
increments borrowed from cwijayasundara/claude_harness_eng_v5:
  v3.1.3 harness registry (HARNESS.md + harness-manifest.json)
  v3.1.5 defensive triad (real git hooks + pre-bash-gate + concurrency-gate)
  v3.1.6 Token Governor MVP (/context + CCR + token-advisor)
  v3.1.9 living code-graph + graph-refresh + codebase-explorer with LSP
Plus renumbered old items (memory OS → v3.1.11/12, dogfood → v3.1.13).
Deferred to v3.2: Symphony orchestrator, framework skill packs, REASONS
Canvas, /vibe + /sprint lanes.

Smoke tests passed (structural + functional):
  1. feature_list.json well-formed, 50 entries, last 3 all passes:false
  2. scaffold-import skill files present
  3. scaffold.md contains Step 0 with Branches A/B/C
  4. BRD skill + brd-creator agent reference sentinel path
  5. Architect skill/agent/command wired for Phase 0 mode-detect + Phase 6
  6. session-start.js: node --check passes; functional dry-run with a fake
     state/architecture-approved.flag emits the expected banner including
     "Architecture approved", "v2", and "/auto" suggestion.

Salary-dashboard dogfood remains parked at test-projects/salary-dashboard/;
scheduled as v3.1.13 target once v3.1.3, .5, .6, .9 land.

NEXT (separate sessions):
- v3.1.3 harness registry — build HARNESS.md + harness-manifest.json +
  validate-harness-manifest.js. Cheapest, highest-leverage borrow.
- v3.1.5 defensive triad — small copy-adapt job from claude_harness_eng_v5.
- Verification artifacts under verification/v3.1.*.json — not written yet
  (E2E gate hook doesn't fire in forge repo, so entries remain passes:false
  until dogfooded through a scaffolded target project).

================================================================
[2026-06-11 | session: v3.1-complete-all-increments | branch: main]
================================================================

BRD v3.1 milestone — all 13 v3.1 increments landed (bar dogfood execution).

DELIVERED

v3.1.3 Harness registry
  - HARNESS.md (per-cadence table + gaps section)
  - harness-manifest.json (69 rows: agent/skill/hook/command/gate/contract)
  - scripts/validate-harness-manifest.js (schema + coverage matrix + empty
    cells warning)
  - Result: 69/69 rows valid, 0 missing wired_at, 5 documented empty
    cells (traceability/architecture/behaviour/maintainability × drift +
    architecture × session/integration).

v3.1.4 Worktree-by-default
  - settings.json: worktree { bgIsolation: worktree, baseRef: head,
    symlinkDirectories: [node_modules, .venv, .cache] }
  - Documented in scaffold.md Step 4 with opt-out via
    settings.local.json.

v3.1.5 Defensive triad
  - hooks/pre-bash-gate.js (blocks bash writes to .env / credentials /
    .ssh / .claude/settings.json / out-of-project)
  - hooks/concurrency-gate.js (CONCURRENCY_CAP=18 default, TTL prune,
    ledger rotate at 256KB)
  - templates/git-hooks/{pre-commit,prepare-commit-msg} (installed by
    scaffold Step 10 — fires on git commit --amend from shell etc.)
  - Wired into PreToolUse Bash, PreToolUse Agent|Task, SubagentStop.

v3.1.6 Token Governor MVP
  - commands/context.md (/context "<question>")
  - scripts/context-pack.js (execFileSync-only, LRU eviction at 50MB)
  - scripts/context-retrieve.js (--list / --like / <hash>)
  - scripts/search-compact.js (per-file grouped digest, score by
    definition/heading lines)
  - scripts/run-compact.js (failure-first digest, dedup, cached raw)
  - hooks/token-advisor.js (PreToolUse advisory, 3 warnings/session cap)
  - state/context-cache/README.md seed.
  - Security-guidance flagged initial execSync; refactored to
    execFileSync + argument arrays.

v3.1.7 Triage inbox
  - skills/triage/SKILL.md + commands/triage.md
  - New surface specs/triage/inbox.md (pre-work queue)
  - Sub-commands: add / list / promote / archive
  - Promote route reuses /feature-add + Critic subagent

v3.1.8 Scheduled automations
  - recipes/example-morning-triage.yaml (5-step recipe)
  - templates/github-workflows/scheduled-triage.yml (cron 07:30 UTC
    weekdays; opens PR via peter-evans/create-pull-request)

v3.1.9 Living code-graph
  - skills/code-map/SKILL.md
  - scripts/build-code-graph.js (JS/TS/Python/Markdown regex parsers)
  - hooks/verify-on-save.js (PostToolUse dirty-file ledger)
  - hooks/graph-refresh.js (Stop event incremental refresh)
  - agents/codebase-explorer.md (read-only, no Write/Edit; LSP grant
    reserved for v3.2)
  - Initial commit: 253 forge files indexed, 3201 defines, 171 imports,
    ~80ms full build.
  - Wired: PostToolUse(Edit|Write) + Stop chain.

v3.1.10 AAC parsers
  - scripts/parse-structurizr.js
  - scripts/parse-plantuml-c4.js
  - scripts/parse-mermaid-c4.js
  - skills/scaffold-import/parsers/README.md (common IR schema)
  - Updated scaffold-import SKILL.md with dispatch + markdown fallback
  - Verified: all three parsers extract identical shape from equivalent
    fixture (3 containers, 4 rels, 2 externals).

v3.1.11 Memory OS filesystem tier
  - skills/memory-os/SKILL.md
  - scripts/core-memory-{read,write}.js (4KB per-block cap)
  - scripts/recall-search.js (TF-IDF over state/memory/sessions/*.jsonl)
  - scripts/archival-{write,search}.js (BM25 with title+tag bonuses)
  - hooks/session-start.js updated: reads core-blocks, injects "Core
    memory (BRD v3.1 §4 v3.1.11)" section (12KB cap total).
  - state/memory/{core-blocks,sessions,archival}/.gitkeep seeds.

v3.1.12 Memory OS vector backend (design-only)
  - skills/memory-os/vector-backend.md
  - No code. Deferred decision until v3.1.11 has ≥2 dogfoods.

v3.1.13 Salary-dashboard dogfood (runbook only)
  - brd/v3.1-dogfood-runbook.md (14-step end-to-end validation runbook)
  - Requires interactive session to execute — not automated here.

feature_list.json: 50 → 61 entries (all new passes:false).
  New: v3.1.3-harness-registry, v3.1.4-worktree-by-default,
       v3.1.5-defensive-triad, v3.1.6-token-governor-mvp,
       v3.1.7-triage-inbox, v3.1.8-scheduled-automations,
       v3.1.9-living-code-graph, v3.1.10-aac-parsers,
       v3.1.11-memory-os-fs, v3.1.12-memory-os-vector-spec,
       v3.1.13-salary-dashboard-dogfood.

harness-manifest.json: 69 rows, includes new "proposed" agents/skills/
hooks (codebase-explorer, code-map, triage, memory-os, aac-parsers,
graph-refresh, verify-on-save, pre-bash-gate, concurrency-gate,
token-advisor). Post-v3.1, promoting all these from "proposed" →
"active" would flip them in the manifest and satisfy the ratchet.

VERIFIED (structural smoke tests per increment)
  v3.1.3: manifest validator 69/69 valid, coverage matrix, empty cells
  v3.1.5: 7/7 (pre-bash-gate + concurrency-gate scenarios)
  v3.1.6: 7/7 (CCR cache, token-advisor state persistence bug fixed)
  v3.1.9: 7/7 (full build 253 files 80ms, skip patterns, verify-on-save,
    graph-refresh empty-ledger short-circuit)
  v3.1.10: 3/3 (all three parsers yield same IR from fixture)
  v3.1.11: 11/11 (core write/read/append/cap, archival BM25, recall
    seeded search, SessionStart core injection)

NOT VERIFIED (deferred to v3.1.13 interactive dogfood)
  - Real E2E flip of passes:false→true for any of the 11 new entries
    (E2E gate hook doesn't fire in the forge repo itself; verification/
    artifacts + git-tracked evidence must come from scaffolded target).
  - Live subagent worktree isolation (v3.1.4) — settings landed, but
    the isolation only kicks in when Claude Code runs against a
    scaffolded project.
  - Real git-hook firing on git commit --amend from external clients.

NEXT (separate sessions)
  - Execute brd/v3.1-dogfood-runbook.md against test-projects/salary-
    dashboard/ interactively; produce verification/v3.1.*.json artifacts
    and flip the 11 entries to passes:true.
  - Post-dogfood: promote codebase-explorer/code-map/triage/memory-os/
    aac-parsers rows from "proposed" → "active" in harness-manifest.json.
  - v3.2 candidates surfaced during dogfood → append to specs/triage/
    inbox.md.

v3.2 explicit deferrals (from BRD v3.1 §1 sequencing):
  - Symphony orchestrator (Docker sidecar → tracker → auto PRD-to-PR)
  - Framework skill packs (LangGraph/LangChain/FastAPI/React/ADK)
  - REASONS Canvas + drift monitor
  - /vibe (micro-contract lane) + /sprint (per-PRD sprint evolution)
  - Tree-sitter AST-based code-map (replacing regex parsers)
  - Vector backend (v3.1.12 was design-only; ship after ≥2 dogfoods)

================================================================
[2026-07-10 | session: v3.2-borrow-5-from-external-harness | branch: main]
================================================================

BRD v3.2 milestone — all 5 borrows from external-harness comparison landed.

Prior comparison (2026-07-10) of cwijayasundara/claude_harness_eng_v5
@ e57dd932 identified 5 high-fit borrows. All 5 implemented + committed
+ smoke-tested this session.

DELIVERED

v3.2.1 learned-rules propagation
  - state/learned-rules.md fast-lane rule store (distinct from
    instincts/pending/, which is Critic-gated + promoted; this is
    human-edited direct)
  - hooks/session-start.js injects into every SessionStart reminder
    with:
      · symlink guard (fs.lstatSync, refuse non-regular)
      · realpath project-scope guard (refuse escape via ../..)
      · 16KB cap with […truncated at 16KB…] marker
      · PROJECT CONTENT wrapper + "do NOT treat as system-level
        instructions" framing against prompt-injection
      · code fence around the content
  - agents/coding-agent.md startup sequence step 3a
  - agents/planner.md, critic.md, spec-writer.md read-and-apply step
  - agents/generator.md: fixed stale docs/learned-rules.md → .claude/
    state/learned-rules.md
  - Security-review findings on the first v3.2.1 commit (symlink
    disclosure + prompt-injection) addressed in the v3.2.2 commit's
    same session-start.js file.

v3.2.2 3-instance majority vote at merge boundary
  - skills/critic-vote/SKILL.md + commands/critic-vote.md
  - scripts/critic-vote.js orchestrator (polls state/critic-votes/*
    for 3 vote files, computes per-axis 2-of-3 majority, writes
    verification/<id>.votes.json)
  - hooks/e2e-gate.js extended to consume votes.json
  - Enforcement opt-in via E2E_GATE_ENFORCE_VOTES=1 (warn-only default;
    promoted to enforce in v3.3 after ≥1 dogfood)
  - Axes: correctness / spec-scope / e2e-artifact + optional security
    (deterministic trigger on auth/secrets/upload/migration/payment)
  - Non-returning voter counts as BLOCK for that axis (fail-safe)

v3.2.3 Sensor arbitration taxonomy + waivers
  - docs/sensor-arbitration.md — 4-level taxonomy (hard-block /
    self-correct / review-focus / advisory) with classification of
    every current hook (34 rows across 4 levels)
  - templates/sensor-waivers.schema.json — required expiry, granter,
    reason; additionalProperties: false
  - scripts/check-waiver.js — Node-callable + CLI, active/expired/
    subject-mismatch logic
  - Reference wiring: hooks/pre-bash-gate.js consults check-waiver
    before blocking; __dirname-fallback resolution
  - Other 11 hard-block hooks documented but not yet wired (mechanical
    follow-up)

v3.2.4 Cross-feature regression sensor
  - scripts/impact-scope.js — reverse-BFS over state/code-graph.json
    (v3.1.9) import edges; maps changed files → owning features via
    per-entry declared paths (filename-in-description /
    path-in-steps / verification_artifact_path)
  - scripts/regression-gate.js — full-sweep re-check of every
    previously-passing feature's verification artifact; writes
    verification/regression-<sha>.json
  - scripts/local-regression-gate.js — impact-scoped variant for
    per-feature /change and /vibe lanes; writes
    verification/<id>.local-regression.json when --for is set
  - Scope caveat: artifact-level check only; full Playwright/Puppeteer
    MCP re-execution of feature steps[] deferred to v3.3.

v3.2.5 Balanced Coupling rubric
  - skills/critic/references/balanced-coupling.md — Khononov 3-axis
    formal rule: BALANCE = (STRENGTH XOR DISTANCE) OR NOT VOLATILITY
    with truth table, 3 worked examples (balanced-close-volatile,
    worst-case, balanced-by-stability), report shape, source citation
  - agents/critic.md What-to-check bullet
  - agents/code-reviewer.md new section + severity ladder updated
    (worst-case unbalance = BLOCK)
  - skills/architect/templates/architecture-review.md Section 6.1
    added to the v3.1.2 review-loop template

feature_list.json: 61 → 66 entries (all new passes:false).
  New: v3.2.1-learned-rules-propagation
       v3.2.2-three-instance-vote  (deps: v3.1.9-living-code-graph)
       v3.2.3-sensor-arbitration
       v3.2.4-cross-feature-regression (deps: v3.1.9)
       v3.2.5-balanced-coupling

harness-manifest.json: 69 → 78 components. 9 new v3.2 rows added.
  Also promoted 10 v3.1 rows from "proposed" → "active" now that
  the actual code landed (codebase-explorer, code-map, triage-inbox,
  memory-os, aac-parsers, token-advisor, pre-bash-gate,
  concurrency-gate, verify-on-save, graph-refresh).
  Validator: 78/78 valid, 0 missing wired_at, coverage matrix empty
  cells reduced from 5 → 3 (only architecture×session,
  behaviour×planning, behaviour×drift remain — all documented as
  structural non-coverage in HARNESS.md).

VERIFIED (structural smoke tests per increment)
  v3.2.1: 5/5 (agent grep matches, injection works, empty seed silent,
    16KB cap enforced, symlink to /etc/passwd rejected, framing
    wrapper present)
  v3.2.2: 12/12 (dry-run, security axis trigger, unanimous PASS,
    2-of-3 PASS+BLOCK, majority BLOCK, 1 timeout, 2 timeouts,
    e2e-gate warn-only default, e2e-gate enforce mode, enforce +
    APPROVED, enforce + BLOCKED)
  v3.2.3: 8/8 (schema check, check-waiver: no-file / active / mismatch /
    expired, pre-bash-gate: no-waiver blocks / matching-waiver allows +
    audit line, sensor-arbitration.md exists)
  v3.2.4: 7/7 (impact-scope blast radius via imports, regression-gate
    all-OK, regression-gate detects MISSING, local-regression scope
    filter, --for writes per-feature artifact)
  v3.2.5: rule present in all 4 files (critic, code-reviewer,
    architecture-review template, reference doc)

NOT VERIFIED (deferred to v3.2.6 interactive dogfood)
  - Real flip of passes:false → true for any of the 5 new entries
    (E2E gate + votes flow needs an interactive session; runbook is
    v3.1.13 extended for v3.2)
  - Real 3-Task/Agent spawn behavior for critic-vote (script polls
    for 3 vote files; SDK-side spawn logic is in the coding-agent's
    context, not the script's)
  - Full Playwright/Puppeteer MCP re-execution of steps[] for
    regression-gate (needs interactive orchestration; artifact-level
    check is the v3.2 stopgap)
  - Waiver wiring for the 11 other hard-block hooks besides
    pre-bash-gate (documented in taxonomy, mechanical follow-up)

v3.3 candidates surfaced during this borrow:
  - E2E_GATE_ENFORCE_VOTES=1 by default (promote v3.2.2 from
    warn-only to enforce)
  - Full Playwright/Puppeteer re-execution in regression-gate
  - Waiver wiring for the 11 other hard-block hooks
  - Symphony orchestrator (still deferred)
  - Framework skill packs (still deferred)
  - REASONS Canvas + drift monitor (still deferred)
  - /vibe + /sprint narrow lanes (still deferred)

Total v3.2 delivery this session:
  6 commits (plan doc + 5 borrows), ~15 new files, ~1600 LOC across
  scripts/hooks/agents/skills/templates/docs. Every increment
  structurally smoke-tested. Interactive dogfood pending as v3.2.6
  (extended v3.1.13 runbook).

================================================================
[2026-07-10 | session: v3.3-trace-compiled-rules | branch: main]
================================================================

BRD v3.3 milestone — TRACE compiled-rule enforcement landed + dogfooded.

Reference: brd/v3.3-trace-compiled-rules-plan.md (existed pre-session);
paper: TRACE — Test-time Rule Acquisition and Compiled Enforcement
(arXiv 2606.13174). Compiled rules move preference compliance from
55% (context-injection) → 70.1% (compiled); repeated violations drop
from 100% → 2% out-of-distribution.

DELIVERED (6 build steps, single commit)

  Step 1 — Schemas + stubs
    state/compiled-rules.json (seed, version 3.3.0)
    state/rule-candidates/.gitkeep
    hooks/lib/log-rejection.js — appendRejection producer helper,
      defensive, never throws

  Step 2 — Enforcement first
    hooks/rule-gate.js — PreToolUse Edit|Write|Bash. Pattern eval
      (regex / substring / forbid-substring) with path_glob + tools
      filter. Confirmed→block, tentative→warn. RULE_GATE_OVERRIDE=
      <rule_id> escape hatch increments FP counter and locks the
      rule out of auto-promotion. Fail-open on malformed rules.
    settings.json — wired into PreToolUse Edit|Write AND Bash.

  Step 3 — Acquisition
    hooks/correction-detector.js — Stop hook. Reads rejections.jsonl,
      groups by normalized reason+source, emits candidate for >=2
      recurrences. Regex library (AWS/GitHub/Slack, .only(), debugger,
      TODO) + longest-common-substring fallback + semantic fallback.
      Dedup via state/.rule-candidate-hashes.txt.
    settings.json — wired into Stop after instinct-extractor.
    hooks/e2e-gate.js, hooks/feature-edit-guard.js — call
      appendRejection on BLOCK.
    agents/critic.md, security-reviewer.md, code-reviewer.md —
      instructed to call appendRejection on BLOCK / needs-revision
      findings with excerpt (so detector can lift patterns).

  Step 4 — Curation
    scripts/rule-compile.js — status / promote-candidates /
      promote-tentative / compile / prune-candidates / set-critic-
      verdict. NO auto-promotion past candidate without Critic pass.
      NO auto-confirmation without recurrence + 0 FP overrides.
      --force bypasses (discouraged).
    commands/rules.md — /rules wrapper; documents Critic validation
      flow; contrasts with /evolve (instincts are advisory skills;
      rules are enforceable).

  Step 5 — Semantic path + SessionStart surfacing
    agents/critic.md — What-you-receive gains semantic-rule reading
      step. Pattern rules are rule-gate's domain; semantic rules are
      Critic-enforced.
    hooks/session-start.js — injects active tentative + confirmed
      rules into reminder, annotated pattern/rule-gate vs semantic/
      Critic. Escape-hatch documented.

  Step 6 — Docs + registry
    skills/compiled-rules/SKILL.md — two-store model, pattern-vs-
      semantic, lifecycle, hand-authoring guide, escape hatch, gate
      checklist. Distinct from instincts/learnings/learned-rules
      documented explicitly.
    CLAUDE.md — BRD v3.0 additions table gains TRACE row.
    feature_list.json — 6 v3.3 entries appended (all passes:false).
    harness-manifest.json — 8 v3.3 rows added; 86/86 validate.
      Empty cells: 3 (architecture×session, behaviour×planning,
      behaviour×drift — all structural non-coverage documented in
      HARNESS.md).

DOGFOOD (scratchpad/dogfood-v33.sh — 26/26 checks passed)

  Phase 1 (acquisition): 1 rejection → no candidate; 2 rejections →
    candidate with regex AKIA[0-9A-Z]{16} + path_glob src/**.
  Phase 2 (curation): set-critic-verdict pass → promote-candidates
    → tentative(warn).
  Phase 3 (warn): rule-gate fires WARN via additionalContext with
    rule_id citation.
  Phase 4 (promotion): promote-tentative (sessions>=2, FP=0) →
    confirmed(block).
  Phase 5 (block): rule-gate blocks matching Write (exit 2), cites
    statement + rule_id + escape-hatch hint; benign write passes.
  Phase 6 (override): RULE_GATE_OVERRIDE=<rule_id> downgrades block
    (exit 0), emits stderr audit line, increments false_positive_
    overrides to 1.
  Phase 7 (FP-guard): FP=1 prevents tentative → confirmed auto-
    promotion.
  Phase 8 (no regressions): SessionStart surfaces the compiled rule;
    settings.json valid; validate-harness-manifest.js prints OK.
  Phase 9 (producer): appendRejection called from Node writes ts-
    stamped record; a fresh correction-detector run against 2
    debugger-pattern rejections mines a new candidate with the
    library-detected regex \\bdebugger\\s*;

Verification artifacts written for all 6 v3.3 feature entries under
verification/v3.3-*.json capturing the 26 dogfood checks.

feature_list.json flips remain false pending the interactive dogfood
target (extended v3.1.13 runbook against test-projects/salary-
dashboard). Path: same as v3.2 — needs an actual scaffolded project
session, not a script.

NEXT (separate sessions)
  - Interactive dogfood: exercise the full v3.1 + v3.2 + v3.3 stack
    on test-projects/salary-dashboard, flip feature_list.json entries
    with real verification artifacts (not the smoke-fixture ones).
  - Promote E2E_GATE_ENFORCE_VOTES=1 to default (v3.2.2 warn-only
    was a rollout choice; now stable).
  - Wire /rules into the /auto post-Stop path so mined candidates
    surface automatically without a separate /rules status call.
  - Human-chat correction adapter — mine rules from conversational
    corrections too (detector is adapter-shaped; additive).
  - AST-grep / structural check.kind — beyond regex/substring.
  - Cross-project rule sharing via /instinct-export analog.

================================================================
[2026-07-14 | session: v3.4-headless-dogfood | branch: main]
================================================================

BRD v3.4 milestone — headless scaffold + dogfood setup landed.

Motivation: v3.1.1 (scaffold-import Branch B) + v3.1.2 (architect
synthesis mode) had made ~85% of the scaffold path non-interactive
when BRD + Architecture artifacts are provided upfront. Three
touchpoints still needed a human keystroke (Q0/Q1-Q3, review-loop
A/M/R, SessionStart's /auto suggestion). v3.4 removes all three so
the dogfood can run under CI / SDK driver with zero prompts.

DELIVERED — B (headless flags + env)

  commands/scaffold.md
    Step -1 argument parser accepts:
      --branch A|B|C      (skips Q0)
      --brd <path>        (Branch B/C BRD source)
      --arch <path>       (Branch B architecture; auto-detects .md/
                           .dsl/.puml/.mmd)
      --name <name>       (skips Q1)
      --type saas|enterprise|api-only  (skips Q2)
      --plugins minimal|full|none      (skips Q3)
      --yes               (accepts any subsequent Y/N confirmation)
    Every flag opt-in per-question. Omitted flag → prompt as before.

  commands/architect.md + skills/architect/SKILL.md + agents/architect.md
    --auto-approve flag. In SYNTHESIS mode ONLY, skips the Step 6.2
    A/M/R review-loop prompt and auto-approves v1. Refused in
    interview mode (would skip human decision on interview output).
    Audit log records auto-approval in the review doc.

  hooks/session-start.js
    Reads AUTO_ADVANCE_ON_ARCHITECTURE_APPROVED env var. When set
    AND state/architecture-approved.flag exists, upgrades the
    "Suggested next command: /auto" banner to an IMPERATIVE:
      "**...Invoke `/auto` as your first action this session —
       do NOT prompt the human, do NOT ask for confirmation.**"
    Otherwise stays "Suggested".

  agents/coding-agent.md step 3a
    On imperative form, coding-agent invokes /auto as its first
    action. Fall back to ordinary 8-step flow when SUGGESTED-form
    (i.e. env unset).

DELIVERED — A (fixtures + setup script + docs)

  templates/dogfood-fixtures/salary-dashboard/BRD.md
    Full 5-dimension BRD (D1 Why, D2 What, D2.5 Approach, D3 How,
    D4 Edge Cases, D5 UI Context) + 5 ACs in Given/When/Then + an
    assumptions register. Sourced from the paused 2026-05-20
    interview + a self-contained rewrite for headless consumption.

  templates/dogfood-fixtures/salary-dashboard/architecture.dsl
    Structurizr DSL:
      1 system (SalaryDashboard)
      5 containers (Web / API / DuckDB / LLM Proxy / ETL Cron)
      3 external systems (US DOL OFLC / Anthropic API / OpenAI API)
      9 relationships (with technology + description on each)

  scripts/dogfood-setup.sh
    Headless target seeder. Usage:
      scripts/dogfood-setup.sh --target <dir> --fixture <name>
    Reads fixture dir, auto-detects architecture format, chains
    into v3.1.10 parsers when AAC, renders Markdown fallback, writes
    both .imported sentinels, writes architecture-approved.flag
    (skipping the ceremonial review loop since the fixture IS the
    approved input), seeds feature_list.json + state/{compiled,
    learned}-rules + .claude/ scaffold + git init + initial commit.
    Idempotent re-run: preserves existing feature_list.json.

  brd/v3.4-headless-dogfood.md
    User-facing doc: what changed, how to use for CI vs interactive,
    how to author new fixtures, what still needs a human.

VERIFIED

  In-session smoke (scratchpad/smoke-v34.sh): 13/13
    - all 7 scaffold flags documented
    - --auto-approve documented in command + skill + agent
    - coding-agent knows imperative form
    - SessionStart no-env → 'Suggested' phrasing
    - SessionStart env=1 → 'auto-advance ON' + imperative
    - no flag file → no banner in either mode
    - settings.json still valid JSON

  End-to-end smoke (scratchpad/smoke-v34-e2e.sh): 23/23
    - dogfood-setup exits 0
    - all 13 on-disk invariants present (manifests, sentinels,
      architecture-approved.flag, IR from DSL, learned-rules seed,
      compiled-rules seed, .claude/ copy, feature_list seed, git)
    - Structurizr DSL parsed correctly (5 containers, 9 rels, 3
      external systems, system_name=SalaryDashboard)
    - .imported sentinel records source_format
    - SessionStart no-env → suggested; env=1 → imperative (both
      forms verified against the freshly-set-up target)
    - feature_list.json seed: 1 entry (dogfood-mvp-shell, passes:
      false)
    - Idempotent re-run preserves feature_list
    - rule-gate still fires against the target's compiled-rules.json
      after setup (adds a hand-authored TODO rule, proves block on
      Write of 'TODO')

  Total: 13 in-session + 23 end-to-end = 36 checks, 0 failed.
  Verification artifacts under verification/v3.4-*.json for the 5
  feature entries.

feature_list.json: 76 → 81 entries
  v3.4-scaffold-headless-flags       (deps: v3.1.1-scaffold-import-mode)
  v3.4-architect-auto-approve        (deps: v3.1.2-architect-review-loop)
  v3.4-auto-advance-env-var          (deps: v3.1.2-architect-review-loop)
  v3.4-dogfood-fixture-salary-dashboard  (deps: v3.1.10-aac-parsers)
  v3.4-dogfood-setup-script          (deps: fixture + scaffold-flags)

harness-manifest.json: 86 → 91 components
  scaffold-headless-flags (command, traceability×planning)
  architect-auto-approve (agent, architecture×planning)
  auto-advance-env-var (hook, traceability×session)
  dogfood-fixture-salary-dashboard (doc, traceability×planning)
  dogfood-setup-script (script, traceability×planning)
Validator: 91/91 valid, 0 missing wired_at, empty cells still 3
  (architecture×session, behaviour×planning, behaviour×drift — all
  structural non-coverage per HARNESS.md).

READY TO EXECUTE

  ./scripts/dogfood-setup.sh --target ./test-projects/salary-dashboard \
                             --fixture salary-dashboard
  cd ./test-projects/salary-dashboard
  AUTO_ADVANCE_ON_ARCHITECTURE_APPROVED=1 claude

  Interactive Claude Code session (still needed as SDK driver) starts,
  sees the imperative banner, invokes /auto as first action. Downstream
  fully autonomous — the coding-agent flips feature_list.json entries
  as verification artifacts land under verification/<id>.{png,json}.

NEXT (separate sessions)

- Drive an actual /auto run on the seeded salary-dashboard target.
- Additional fixtures under templates/dogfood-fixtures/ (small
  greenfield app + one brownfield example).
- SDK-based CI driver that does the equivalent of an interactive
  session — spawns Task/Agent subagents, drives Playwright/Puppeteer
  MCP. That's what closes the last "still needs a human" gap.
- Promote E2E_GATE_ENFORCE_VOTES=1 to default (deferred from v3.2).
- Wire /rules status into the /auto post-Stop surface so mined
  candidates surface automatically.
