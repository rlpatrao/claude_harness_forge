---
name: build
description: Full 12-phase SDLC pipeline. BRD → Architect → Spec → Design → Observe → Comply → Initialize → Auto (11 gates) → Post-build. Human gates on phases 1-4.
argument-hint: "[path-to-BRD] [--mode full|lean|solo|turbo]"
---

# /build — Full SDLC Pipeline

When the user runs this command, read and follow `.claude/skills/build/SKILL.md` exactly.

Pass through any arguments (e.g. BRD path, `--mode <mode>`).
