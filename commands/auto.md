---
name: auto
description: Autonomous build loop with Karpathy ratcheting, GAN evaluator, browser console capture, UI standards review, 8-gate ratchet, session chaining, and cross-project learnings.
argument-hint: "[--mode full|lean|solo|turbo] [--group GROUP_ID]"
---

# /auto — Autonomous Build Loop

When the user runs this command, read and follow `.claude/skills/auto/SKILL.md` exactly.

Pass through any arguments (e.g. `--mode <mode>`, `--group <id>`).
